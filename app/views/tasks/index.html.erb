<% @tasks.each do |task| %>
  <%= link_to task_path(task) do %>
    <h2><%= task.name %></h2>
  <% end %>
  <ul>
    <li><%= task.content %>
    </br>
    <%= task.created_at %></li>
  </ul>
<% end %>

<%= link_to "New task", new_task_path(@task) %>
